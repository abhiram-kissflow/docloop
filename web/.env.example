DATABASE_URL=
GITHUB_WEBHOOK_SECRET=
GENERIC_HOOK_TOKEN=
WORKER_API_KEY=
# One shared password for the whole documentation team. Unset means the
# dashboard denies everyone — the gate never fails open.
DASHBOARD_PASSWORD=
